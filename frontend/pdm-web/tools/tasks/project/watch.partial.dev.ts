import { watchPartial } from '../../utils/project/watch.partial';

/**
 * Executes the build process, watching for file changes and rebuilding the development environment.
 */
export = watchPartial();
